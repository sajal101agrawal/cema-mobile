import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const useAppState = () => {
  return (
    <View>
      <Text>useAppState</Text>
    </View>
  )
}

export default useAppState

const styles = StyleSheet.create({})